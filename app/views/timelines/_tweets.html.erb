<%- for tweet in @timeline %>
  <%= render tweet %>
<% end %>

<div id="get_more">
  <%= link_to "get more", @get_more_url, :remote => true, :id => "get_more_link" , :method => :get if @get_more_url && @page.to_i <= 10 %>
</div>
